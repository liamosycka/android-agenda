<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.labprogtest01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Agenda"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AgregarContacto"
            android:label="Agregar contacto"></activity>
        <activity
            android:name=".Opciones"
            android:label="Opciones"></activity>
        <activity
            android:name=".ListaContactos"
            android:label="Contactos" ></activity>
        <activity
            android:name=".ListaCumple"
            android:label="Cumpleaños" ></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>